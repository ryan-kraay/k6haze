name: Terraform Deploy on Github
run-name: "Deploy Github ${{github.ref_name}} #${{github.run_attempt}}"

on:
  push: {}
#    branches: ["main"]
#    paths:
#      - terraform/github/**
  workflow_dispatch:
    inputs: {}

#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:

permissions:
  actions: write
  contents: write
  issues: write
  pull-requests: write
  statuses: read
  #contents: read
  #environments: write
  #secrets: write
  #variables: write

jobs:
  deploy-github:
    name: Terraform
    runs-on: ubuntu-latest
    environment: RKurator
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42 # v2.1.4
        id: app-token
        with:
          app-id: "${{ secrets.BOT_APP_ID2 }}"
          private-key: "${{ secrets.BOT_APP_PRIVATE_KEY2 }}"

      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          token: "${{ steps.app-token.outputs.token }}"

      - name: Install Devbox
        uses: jetify-com/devbox-install-action@a0d2d53632934ae004f878c840055956d9f741b0 # v0.14.0
        with:
          enable-cache: 'true'
          devbox-version: "0.16.0"

      - name: Create Wrapper
        uses: ./.github/actions/capture-wrapper
        with:
          command: "devbox"
          wrapper: "devbox-w"

      - name: Syntax Formatting
        id: fmt
        env:
          AWS_ACCESS_KEY_ID: "${{ secrets.AWS_ACCESS_KEY_ID }}"
          AWS_SECRET_ACCESS_KEY: "${{ secrets.AWS_SECRET_ACCESS_KEY }}"
          GITHUB_TOKEN: "${{ steps.app-token.outputs.token }}"
          SOPS_AGE_KEY: "${{ secrets.SOPS_AGE_KEY }}"
          TF_VAR_cloudflare_account_id: "${{ secrets.TF_VAR_cloudflare_account_id }}"
          TF_VAR_terraform_statefile_bucket: "${{ secrets.TF_VAR_terraform_statefile_bucket }}"
          TF_VAR_terraform_statefile_passphrase: "${{ secrets.TF_VAR_terraform_statefile_passphrase }}"
          TF_VAR_github_owner_slug: "${{ github.repository_owner }}"
        run: devbox-w run tf-github fmt -recursive -check .
        continue-on-error: true

      - name: Initialize
        id: init
        env:
          AWS_ACCESS_KEY_ID: "${{ secrets.AWS_ACCESS_KEY_ID }}"
          AWS_SECRET_ACCESS_KEY: "${{ secrets.AWS_SECRET_ACCESS_KEY }}"
          GITHUB_TOKEN: "${{ steps.app-token.outputs.token }}"
          SOPS_AGE_KEY: "${{ secrets.SOPS_AGE_KEY }}"
          TF_VAR_cloudflare_account_id: "${{ secrets.TF_VAR_cloudflare_account_id }}"
          TF_VAR_terraform_statefile_bucket: "${{ secrets.TF_VAR_terraform_statefile_bucket }}"
          TF_VAR_terraform_statefile_passphrase: "${{ secrets.TF_VAR_terraform_statefile_passphrase }}"
          TF_VAR_github_owner_slug: "${{ github.repository_owner }}"
        run: devbox-w run tf-github init -input=false

      - name: Validate
        id: validate
        env:
          AWS_ACCESS_KEY_ID: "${{ secrets.AWS_ACCESS_KEY_ID }}"
          AWS_SECRET_ACCESS_KEY: "${{ secrets.AWS_SECRET_ACCESS_KEY }}"
          GITHUB_TOKEN: "${{ steps.app-token.outputs.token }}"
          SOPS_AGE_KEY: "${{ secrets.SOPS_AGE_KEY }}"
          TF_VAR_cloudflare_account_id: "${{ secrets.TF_VAR_cloudflare_account_id }}"
          TF_VAR_terraform_statefile_bucket: "${{ secrets.TF_VAR_terraform_statefile_bucket }}"
          TF_VAR_terraform_statefile_passphrase: "${{ secrets.TF_VAR_terraform_statefile_passphrase }}"
          TF_VAR_github_owner_slug: "${{ github.repository_owner }}"
        run: devbox-w run tf-github validate

      - name: Plan
        id: plan
        needs: init
        env:
          AWS_ACCESS_KEY_ID: "${{ secrets.AWS_ACCESS_KEY_ID }}"
          AWS_SECRET_ACCESS_KEY: "${{ secrets.AWS_SECRET_ACCESS_KEY }}"
          GITHUB_TOKEN: "${{ steps.app-token.outputs.token }}"
          SOPS_AGE_KEY: "${{ secrets.SOPS_AGE_KEY }}"
          TF_VAR_cloudflare_account_id: "${{ secrets.TF_VAR_cloudflare_account_id }}"
          TF_VAR_terraform_statefile_bucket: "${{ secrets.TF_VAR_terraform_statefile_bucket }}"
          TF_VAR_terraform_statefile_passphrase: "${{ secrets.TF_VAR_terraform_statefile_passphrase }}"
          TF_VAR_github_owner_slug: "${{ github.repository_owner }}"
        run: devbox run tf-github plan -input=false
        continue-on-error: true

#        # TODO:
#        #   check outputs for this event
#        #   maybe add: - uses: hashicorp/setup-terraform@v3
#      - name: Update Pull Request
#        # Insipired by: https://github.com/marketplace/actions/hashicorp-setup-terraform#outputs
#        uses: actions/github-script@v7
#        if: github.event_name == 'pull_request'
#        env:
#          PLAN: "terraform\n${{ steps.plan.outputs.stdout }}"
#        with:
#          github-token: ${{ secrets.GITHUB_TOKEN }}
#          script: |
#            // 1. Retrieve existing bot comments for the PR
#            const { data: comments } = await github.rest.issues.listComments({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              issue_number: context.issue.number,
#            })
#            const botComment = comments.find(comment => {
#              return comment.user.type === 'Bot' && comment.body.includes('Terraform Format and Style')
#            })
#
#            // 2. Prepare format of the comment
#            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
#            #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
#            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
#            <details><summary>Validation Output</summary>
#
#            \`\`\`\n
#            ${{ steps.validate.outputs.stdout }}
#            \`\`\`
#
#            </details>
#
#            #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`
#
#            <details><summary>Show Plan</summary>
#
#            \`\`\`\n
#            ${process.env.PLAN}
#            \`\`\`
#
#            </details>
#
#            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`, Working Directory: \`${{ env.tf_actions_working_dir }}\`, Workflow: \`${{ github.workflow }}\`*`;
#
#            // 3. If we have a comment, update it, otherwise create a new one
#            if (botComment) {
#              github.rest.issues.updateComment({
#                owner: context.repo.owner,
#                repo: context.repo.repo,
#                comment_id: botComment.id,
#                body: output
#              })
#            } else {
#              github.rest.issues.createComment({
#                issue_number: context.issue.number,
#                owner: context.repo.owner,
#                repo: context.repo.repo,
#                body: output
#              })
#            }
#
##      - name: Publish Test Results
##        uses: EnricoMi/publish-unit-test-result-action@34d7c956a59aed1bfebf31df77b8de55db9bbaaf # v2.21.0
##        if: always()
##        with:
##          check_run: false
##          files: |
##            report/*.xml
